# Career Intelligence Platform — backend dependencies.
#
# NOTE: torch is intentionally NOT listed here. sentence-transformers pulls it
# in as a dependency; on Linux that would install the ~2.5 GB CUDA build. The
# Dockerfile installs the CPU-only torch from the PyTorch index first, so the
# CUDA wheel is never resolved. On Windows, `pip install -r requirements.txt`
# installs the CPU wheel automatically.

# Application layer
fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy>=2.0
pydantic-settings>=2.6

pydantic>=2.11
langgraph>=1.2
langsmith>=0.4
pygithub>=2.5
httpx>=0.27
numpy>=2.0
sentence-transformers>=5.0
